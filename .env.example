### Ejemplo de variables de entorno (NO COMMIT)
# Copia este archivo a `.env` y completa con tus valores locales/secretos

# Clave secreta para Flask (sesiones) - usar un valor fuerte en producción
SECRET_KEY=changeme_to_a_strong_secret

# URL de tu proyecto Supabase
# URL de tu proyecto Supabase
SUPABASE_URL=https://your-project.supabase.co

# Clave pública/anon para operaciones de autenticación (login)
# Requiere la anon key si quieres permitir que los usuarios inicien sesión.
SUPABASE_ANON_KEY=your_anon_key_here

# Service role key (recomendado para operaciones admin/lectura protegida)
# No lo subas al repo: usa variable de entorno en la nube (heroku, render, fly.io, etc.)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Alternativa: si usas la anon/public key, puedes setear SUPABASE_KEY
# SUPABASE_KEY=your_anon_key_here
